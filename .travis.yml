language: c

os:
 - linux

compiler:
 - clang
 - gcc
 - g++

sudo: required
dist: trusty

addons:
  apt:
    packages:
    - libjack-jackd2-dev
    - libsoxr-dev
    - libgstreamer1.0-dev

before_script:
 - autoreconf -ivf
